# 🔧 إعداد OpenAI API - دليل سريع

## المشكلة الحالية
الخطأ: **503 Service Unavailable**
السبب: الـ OpenAI API key مش موجود أو مش صحيح

## الحل المؤقت ✅
تم إضافة **Basic Summarization** يشتغل بدون OpenAI:
- يستخرج أول 3 جمل كملخص
- يحدد النقاط المهمة
- يستخرج الكلمات الأكثر تكراراً

## للحصول على تلخيص أفضل بـ AI

### 1. احصل على OpenAI API Key

1. اذهب إلى: https://platform.openai.com/api-keys
2. سجل دخول أو أنشئ حساب
3. اضغط "Create new secret key"
4. انسخ الـ key (يبدأ بـ `sk-...`)

### 2. أضف الـ Key للمشروع

افتح ملف `.env.local` وأضف:

```env
OPENAI_API_KEY=sk-your-actual-key-here
OPENAI_MODEL=gpt-4-turbo-preview
```

### 3. أعد تشغيل السيرفر

```bash
# أوقف السيرفر (Ctrl+C)
# ثم شغله تاني
npm run dev
```

## كيف يعمل النظام الآن؟

### مع OpenAI (إذا كان الـ key موجود):
```
✅ تلخيص ذكي بالـ AI
✅ نقاط رئيسية دقيقة
✅ كلمات مفتاحية ذكية
```

### بدون OpenAI (Fallback):
```
✅ تلخيص أساسي (أول 3 جمل)
✅ نقاط مهمة (جمل تحتوي كلمات مهمة)
✅ كلمات مفتاحية (الأكثر تكراراً)
```

## اختبار النظام

1. ارفع مستند PDF
2. اضغط "✨ AI Summarize"
3. شوف الـ Console:

```
⚠️ OpenAI API key not configured, using basic summarization...
✅ Summary generated
```

## الملفات المعدلة
- ✅ `app/api/documents/[id]/summarize/route.ts`

## النتيجة
✅ النظام يشتغل الآن بدون OpenAI
✅ لو عايز AI أفضل → أضف الـ API key
✅ مفيش أخطاء 503 تاني
