# 🔄 Authentication Flow - Visual Guide

## 📊 Complete System Architecture

```
┌─────────────────────────────────────────────────────────────────┐
│                     AUTHENTICATION SYSTEM                        │
└─────────────────────────────────────────────────────────────────┘

┌──────────────┐     ┌──────────────┐     ┌──────────────┐
│   Frontend   │────▶│   API Routes │────▶│   MongoDB    │
│   (React)    │◀────│   (Next.js)  │◀────│   (Atlas)    │
└──────────────┘     └──────────────┘     └──────────────┘
       │                     │                     │
       │                     │                     │
   localStorage          JWT Utils           User Model
   AuthContext          Password Utils
```

---

## 🔐 Signup Flow

```
┌─────────────┐
│   User      │
│  /signup    │
└──────┬──────┘
       │
       │ 1. Fill form (name, email, password)
       ▼
┌─────────────────────────────────────────┐
│  Frontend Validation                    │
│  ✓ Name min 2 chars                     │
│  ✓ Valid email format                   │
│  ✓ Password min 6 chars                 │
│  ✓ Passwords match                      │
└──────┬──────────────────────────────────┘
       │
       │ 2. POST /api/auth/signup
       ▼
┌─────────────────────────────────────────┐
│  API Route: signup/route.ts             │
│  ✓ Validate inputs                      │
│  ✓ Check email exists                   │
│  ✓ Hash password (bcrypt)               │
│  ✓ Save to MongoDB                      │
│  ✓ Generate JWT token                   │
└──────┬──────────────────────────────────┘
       │
       │ 3. Return { success, token, user }
       ▼
┌─────────────────────────────────────────┐
│  AuthContext                            │
│  ✓ Store token in state                 │
│  ✓ Store user in state                  │
│  ✓ Save to localStorage                 │
└──────┬──────────────────────────────────┘
       │
       │ 4. Redirect to /dashboard
       ▼
┌─────────────────────────────────────────┐
│  Dashboard                              │
│  ✓ Show welcome message                 │
│  ✓ Fetch user data                      │
│  ✓ Display statistics                   │
└─────────────────────────────────────────┘
```

---

## 🔑 Login Flow

```
┌─────────────┐
│   User      │
│  /login     │
└──────┬──────┘
       │
       │ 1. Enter credentials
       ▼
┌─────────────────────────────────────────┐
│  Frontend Validation                    │
│  ✓ Valid email format                   │
│  ✓ Password not empty                   │
└──────┬──────────────────────────────────┘
       │
       │ 2. POST /api/auth/login
       ▼
┌─────────────────────────────────────────┐
│  API Route: login/route.ts              │
│  ✓ Find user by email                   │
│  ✓ Compare password (bcrypt)            │
│  ✓ Generate JWT token                   │
└──────┬──────────────────────────────────┘
       │
       │ 3. Return { success, token, user }
       ▼
┌─────────────────────────────────────────┐
│  AuthContext                            │
│  ✓ Store token in state                 │
│  ✓ Store user in state                  │
│  ✓ Save to localStorage                 │
└──────┬──────────────────────────────────┘
       │
       │ 4. Redirect to /dashboard
       ▼
┌─────────────────────────────────────────┐
│  Dashboard                              │
│  ✓ GET /api/dashboard/summary           │
│  ✓ Send Authorization: Bearer <token>   │
│  ✓ Display user data                    │
└─────────────────────────────────────────┘
```

---

## 🔄 Forgot Password Flow

```
┌─────────────┐
│   User      │
│  /forgot-   │
│  password   │
└──────┬──────┘
       │
       │ 1. Enter email
       ▼
┌─────────────────────────────────────────┐
│  Step 1: Request OTP                    │
│  POST /api/auth/forgot-password         │
│  ✓ Find user by email                   │
│  ✓ Generate 6-digit OTP                 │
│  ✓ Save OTP + expiry to DB              │
│  ✓ Send OTP via email                   │
└──────┬──────────────────────────────────┘
       │
       │ 2. User receives OTP
       ▼
┌─────────────────────────────────────────┐
│  Step 2: Enter OTP & New Password       │
│  ✓ User enters OTP                      │
│  ✓ User enters new password             │
│  ✓ User confirms password               │
└──────┬──────────────────────────────────┘
       │
       │ 3. POST /api/auth/reset-password
       ▼
┌─────────────────────────────────────────┐
│  API Route: reset-password/route.ts     │
│  ✓ Verify OTP matches                   │
│  ✓ Check OTP not expired                │
│  ✓ Hash new password                    │
│  ✓ Update user password                 │
│  ✓ Clear OTP fields                     │
└──────┬──────────────────────────────────┘
       │
       │ 4. Redirect to /login
       ▼
┌─────────────────────────────────────────┐
│  Login Page                             │
│  ✓ User logs in with new password       │
└─────────────────────────────────────────┘
```

---

## 🔒 Protected Route Access

```
┌─────────────┐
│   User      │
│  Dashboard  │
└──────┬──────┘
       │
       │ 1. Request protected data
       ▼
┌─────────────────────────────────────────┐
│  Frontend                               │
│  GET /api/dashboard/summary             │
│  Headers:                               │
│    Authorization: Bearer <token>        │
└──────┬──────────────────────────────────┘
       │
       │ 2. API receives request
       ▼
┌─────────────────────────────────────────┐
│  API Route: dashboard/summary/route.ts  │
│  ✓ Extract token from header            │
│  ✓ Verify JWT signature                 │
│  ✓ Check token not expired              │
│  ✓ Extract user ID from token           │
└──────┬──────────────────────────────────┘
       │
       ├─── Valid Token ───┐
       │                   │
       ▼                   ▼
┌──────────────┐    ┌──────────────┐
│ Fetch Data   │    │ Return 401   │
│ Return 200   │    │ Unauthorized │
└──────────────┘    └──────────────┘
```

---

## 💾 Data Storage

```
┌─────────────────────────────────────────┐
│  Browser (localStorage)                 │
│  ┌─────────────────────────────────┐   │
│  │ token: "eyJhbGciOiJIUzI1NiI..." │   │
│  │ user: {                         │   │
│  │   id: "507f1f77bcf86cd799..."   │   │
│  │   name: "John Doe"              │   │
│  │   email: "john@example.com"     │   │
│  │   role: "user"                  │   │
│  │ }                               │   │
│  └─────────────────────────────────┘   │
└─────────────────────────────────────────┘

┌─────────────────────────────────────────┐
│  MongoDB (Database)                     │
│  ┌─────────────────────────────────┐   │
│  │ User Collection                 │   │
│  │ {                               │   │
│  │   _id: ObjectId("507f1f77...")  │   │
│  │   name: "John Doe"              │   │
│  │   email: "john@example.com"     │   │
│  │   password: "$2a$10$..."        │   │
│  │   role: "user"                  │   │
│  │   resetOTP: "123456"            │   │
│  │   resetOTPExpires: Date         │   │
│  │   createdAt: Date               │   │
│  │   updatedAt: Date               │   │
│  │ }                               │   │
│  └─────────────────────────────────┘   │
└─────────────────────────────────────────┘
```

---

## 🔐 JWT Token Structure

```
┌─────────────────────────────────────────┐
│  JWT Token                              │
│  eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9. │
│  eyJpZCI6IjUwN2YxZjc3YmNmODZjZDc5OTQz │
│  OTAxMSIsImVtYWlsIjoiam9obkBleGFtcGxl │
│  LmNvbSIsInJvbGUiOiJ1c2VyIiwiaWF0Ijox │
│  NjQwOTk1MjAwLCJleHAiOjE2NDE2MDAwMDB9 │
│  .SflKxwRJSMeKKF2QT4fwpMeJf36POk6yJV_ │
│  adQssw5c                               │
└─────────────────────────────────────────┘
           │
           ├─── Header ────────────────┐
           │    {                      │
           │      "alg": "HS256",      │
           │      "typ": "JWT"         │
           │    }                      │
           │                           │
           ├─── Payload ───────────────┤
           │    {                      │
           │      "id": "507f1f77...", │
           │      "email": "john@...", │
           │      "role": "user",      │
           │      "iat": 1640995200,   │
           │      "exp": 1641600000    │
           │    }                      │
           │                           │
           └─── Signature ─────────────┘
                HMACSHA256(
                  base64UrlEncode(header) + "." +
                  base64UrlEncode(payload),
                  JWT_SECRET
                )
```

---

## 🛡️ Security Layers

```
┌─────────────────────────────────────────┐
│  Layer 1: Frontend Validation           │
│  ✓ Email format                         │
│  ✓ Password length                      │
│  ✓ Required fields                      │
└──────┬──────────────────────────────────┘
       │
       ▼
┌─────────────────────────────────────────┐
│  Layer 2: API Validation                │
│  ✓ Input sanitization                   │
│  ✓ Email uniqueness                     │
│  ✓ Password strength                    │
└──────┬──────────────────────────────────┘
       │
       ▼
┌─────────────────────────────────────────┐
│  Layer 3: Password Security             │
│  ✓ Bcrypt hashing (10 rounds)           │
│  ✓ Salt generation                      │
│  ✓ Never store plain text               │
└──────┬──────────────────────────────────┘
       │
       ▼
┌─────────────────────────────────────────┐
│  Layer 4: JWT Security                  │
│  ✓ Signed with secret key               │
│  ✓ 7-day expiration                     │
│  ✓ Verified on every request            │
└──────┬──────────────────────────────────┘
       │
       ▼
┌─────────────────────────────────────────┐
│  Layer 5: Rate Limiting                 │
│  ✓ Max 5 password reset attempts        │
│  ✓ 15-minute lockout                    │
│  ✓ OTP expiration                       │
└─────────────────────────────────────────┘
```

---

## 📱 User Experience Flow

```
New User Journey:
┌────────┐   ┌────────┐   ┌────────┐
│ Signup │──▶│Dashboard│──▶│ Explore│
└────────┘   └────────┘   └────────┘
    ↓
  Welcome!
  Account created
  Auto-logged in

Returning User Journey:
┌────────┐   ┌────────┐   ┌────────┐
│ Login  │──▶│Dashboard│──▶│ Work   │
└────────┘   └────────┘   └────────┘
    ↓
  Welcome back!
  Data loaded
  Ready to use

Forgot Password Journey:
┌────────┐   ┌────────┐   ┌────────┐   ┌────────┐
│Request │──▶│Enter   │──▶│ Reset  │──▶│ Login  │
│  OTP   │   │  OTP   │   │Password│   │        │
└────────┘   └────────┘   └────────┘   └────────┘
    ↓            ↓            ↓            ↓
  Email sent   Check email  Password    Success!
               Enter code   updated
```

---

## 🔄 State Management

```
┌─────────────────────────────────────────┐
│  AuthContext (Global State)             │
│  ┌─────────────────────────────────┐   │
│  │ State:                          │   │
│  │  - user: User | null            │   │
│  │  - token: string | null         │   │
│  │  - loading: boolean             │   │
│  │  - isAuthenticated: boolean     │   │
│  │                                 │   │
│  │ Methods:                        │   │
│  │  - signup(name, email, pass)    │   │
│  │  - login(email, pass)           │   │
│  │  - logout()                     │   │
│  │  - refreshUser()                │   │
│  │  - setAuthData(token, user)     │   │
│  └─────────────────────────────────┘   │
└─────────────────────────────────────────┘
           │
           ├─── Used by ───┐
           │               │
           ▼               ▼
    ┌──────────┐    ┌──────────┐
    │  Login   │    │Dashboard │
    │  Page    │    │  Page    │
    └──────────┘    └──────────┘
           │               │
           ▼               ▼
    ┌──────────┐    ┌──────────┐
    │  Signup  │    │ Profile  │
    │  Page    │    │  Page    │
    └──────────┘    └──────────┘
```

---

## ✅ Success Indicators

```
Signup Success:
✓ User created in database
✓ Password hashed
✓ JWT token generated
✓ Token stored in localStorage
✓ User redirected to dashboard
✓ Dashboard shows user name

Login Success:
✓ Credentials verified
✓ JWT token issued
✓ Token stored in localStorage
✓ User redirected to dashboard
✓ Dashboard loads user data
✓ No 401 errors

Forgot Password Success:
✓ OTP generated
✓ OTP saved to database
✓ Email sent (logged in console)
✓ OTP verified correctly
✓ Password updated
✓ User can login with new password
```

---

## 🎯 Testing Checkpoints

```
Test 1: Signup
  Input: name, email, password
  Expected: 201, token, user
  Verify: User in database

Test 2: Duplicate Signup
  Input: same email
  Expected: 409, error message
  Verify: No duplicate created

Test 3: Login
  Input: email, password
  Expected: 200, token, user
  Verify: Token valid

Test 4: Invalid Login
  Input: wrong password
  Expected: 401, error message
  Verify: No token issued

Test 5: Protected Route
  Input: valid token
  Expected: 200, user data
  Verify: Data returned

Test 6: Unauthorized Access
  Input: no token
  Expected: 401, error message
  Verify: Access denied

Test 7: Forgot Password
  Input: email
  Expected: 200, success message
  Verify: OTP in database

Test 8: Dashboard Summary
  Input: valid token
  Expected: 200, dashboard data
  Verify: Data structure correct
```

---

This visual guide shows exactly how your authentication system works at every step!
